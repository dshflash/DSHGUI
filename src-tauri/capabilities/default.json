{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the shell window. The remote harness UI never receives any Tauri permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
